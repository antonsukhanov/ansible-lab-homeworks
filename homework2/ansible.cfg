[defaults]
inventory = inventory
remote_user = ansible
host_key_checking = false
vault_password_file = vault_password

[privilege_escalation]
become = true
become_method = sudo
become_user = root
become_ask_pass = false